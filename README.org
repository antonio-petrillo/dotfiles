#+TITLE: My Dotfiles
#+AUTHOR: Antonio Petrillo

* What this repo contains
+ HerbstluftWM with (derived from standard arcolinux-hlwm config):
  + picom
  + nmtray
  + polybar
  + literal config
+ Xmonad config with
  + literate config
  + xmobar
+ Emacs with
  + evil and other stuff
  + logo folder for the dashboard
  + literal config
+ Awesome config with
  + lua battery widget
+ Vim
+ tmux (need some tweaks)
+ wallpaper collection
+ Alacritty
    
* What I want to add, or redo in a better way
+ A better Vim config
+ Extend emacs/awesome/xmonad config
+ DWM config
+ A folder with all my scripts
+ A NixOS config if I take the courage to take the leap
+ Add a literate config for aweome wm
